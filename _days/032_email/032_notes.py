import datetime as dt

if False:
    current_time = dt.datetime.now()
    current_year = current_time.year
    current_day = current_time.weekday()

if True:
    a_date = dt.datetime(year=1900,month=1,day=1)
    print(a_date)